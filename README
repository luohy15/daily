数组
3 无重复字符的最长子串 滑动窗口，如果窗口没有重复，则延伸，否则收缩
8 字符串转换整数（atoi）处理边界情况
15 三数之和（找出数组中所有3数之和为0的三元组） 排序+双指针，枚举第一个数，剩下两个数用双指针
75 颜色分类（只包含012的数组原地排序）把0扫到左边，把2扫到右边
76 最小覆盖子串（找字符串s中能覆盖字符串t的最小子串）滑动窗口，如果窗口没有覆盖，则延伸，否则收缩
78 子集（数组元素互不相同，求子集）不同的二进制组合对应子集
88 合并两个有序数组 从后往前合并
93 复原IP地址（从一个只包含数字的字符串中解析有效的IP地址）暴力枚举四个部分的长度
189 旋转数组（将数组元素向右移动k个未知）旋转3次

链表
2 两数相加 竖式加法，对较短的数字进行补0操作
21 合并两个有序链表 递归合并或者迭代合并
23 合并K个有序链表 暴力合并，或使用优先级队列合并
25 K个一组翻转链表 递归翻转或使用栈翻转，或直接模拟
83 删除排序链表中的重复元素 cur.next=cur.next.next
141 环形链表（判断链表是否有环）使用快慢指针，如果相遇则有环
146 LRU缓存机制（实现最近最少使用缓存机制） 哈希表+双向链表，实现链表删除和尾部添加操作
206 反转链表 递归反转或者迭代反转
328 奇偶链表 模拟连接odd和even

字典
49 字母异位词分组（字符数量相同只有顺序不同叫做字母异位词） word字符排序作为字典的key

单调栈
42 接雨水（给出一些柱子高度，求能接多少雨水）用栈维护单调非增柱子的下标，弹出某个柱子时计算距离及高度得到容量

优先级队列
23 合并K个有序链表 使用优先级队列维护K个队列中的最小值
347 前K个高频元素 使用优先级队列找到前K大数及对应元素
451 根据字符出现频率排序 使用优先级队列找到前K大数及对应元素
912 排序数据 堆排序，维护大顶堆，每次堆规模缩小1
1801 积压订单中的订单总数（模拟股票买卖订单，求最后积压的订单数） 分别使用大顶堆和小顶堆维护买和卖队列，通过堆顶判断是否发生交易

二叉树
94 二叉树的中序遍历 递归或者迭代
98 验证二叉搜索树 递归验证或者中序遍历的同时记录上一个节点的值
101 对称二叉树 从root开始一个向左一个向右，递归判断
102 二叉树的层序遍历 使用BFS
103 二叉树的锯齿形层序遍历 使用BFS，维护flag表示正序或逆序
105 从前序和中序遍历序列构造二叉树 前序遍历的root是第一个元素，找到中序遍历中的对应元素，递归构建左右子树
112 路径总和（判断是否存在从根到叶子的路径总和为目标值）递归判断或者BFS记录当前节点和
129 求根到叶子节点数字之和（每条根到叶子的路径都代表一个数字）递归，如果是叶子节点就返回数字
226 翻转二叉树 递归翻转左右两棵子树并交换位置
236 二叉树的最近公共祖先 递归判断（两种判断条件：分别在左右子树中，左右子树+当前节点），存储父节点（标记p到root的路径，q到root的路上遇到标记就返回）
lcof54 二叉搜索树的第K大节点 反向中序遍历，第K个就是结果

图
79 单词搜索（图中每个位置一个字母，搜索目标单词） DFS，标记已走过的位置
200 岛屿数量（求图中连通块的个数）使用并查集，DFS或BFS
207 课程表（给出课程及先修关系，要求判断能否修完（是否有环））DFS，每个节点三种状态（未搜索，搜索中，已完成），碰到搜索中的节点则有环
210 课程表2（给出课程及先修关系，要求给出学习顺序（拓扑排序））DFS，每个节点三种状态（未搜索，搜索中，已完成），已完成后将节点加入结果中

模拟
54 螺旋矩阵（顺时针螺旋遍历矩阵）记录当前方向，如果不能继续前进则顺时针旋转
232 使用栈实现队列 维护输入栈和输出栈，输出栈为空时可以将输入栈元素移到输出栈

动态规划
5 最长回文子串 转移方程P(i,j)=P(i+1,j-1)&&(Si==Sj)，边界情况P(i,i)=true,P(i,i+1)=(Si==Si+1)
53 最大子序和（找最大和的连续子数组） 转移方程f(i) = max{f(i-1)+nums[i], nums[i]} 
70 爬楼梯（算爬到第x级台阶的方案数） 转移方程f(x)=f(x-1)+f(x-2)，边界情况f(0)=1,f(1)=1
139 单词拆分（判断单词能不能被拆成都在字典中出现的几部分）转移方程dp(i)=dp(j) and s(j,i) in dict，边界情况dp[0]=true
221 最大正方形（01矩阵找到只包含1的最大正方形）转移方程dp(i,j)=min(dp(i-1,j),dp(i-1,j-1),dp(i,j-1))+1
1800 最大升序子数组和（找最大和的升序子数组）转移方程f(i)=f(i-1)+nums[i] or nums[i]

分治
53 最大子序和（找最大和的连续子数组） 类似线段树的思想pushup(get(nums, l, m), get(nums, m+1, r))
69 x的平方根 二分查找，保持mid*mid<=x的性质
215 数组中的第K个最大元素 快速排序中的分区操作结合二分判断
912 排序数组 快速排序（分区操作），归并排序（合并两个有序数组）

贪心
300 最长上升子序列 使上升序列尽量长（保持上升序列的末尾元素尽量小 ）通过二分查找覆盖掉不小于nums[i]的第一个元素

数学
29 两数相除（不使用乘除mod运算，忽略余数）生成除数的2的n次方列表，从大到小依次与被除数对比，如果可以减则减掉
43 字符串相乘（大数乘法）竖式乘法，把i，j位置的数相乘后累计到i+j位置，统一处理进位
69 x的平方根 牛顿迭代 xi = x0 - f(x0) / f'(x0)
70 爬楼梯（算爬到第x级台阶的方案数） 矩阵乘法快速幂或通项公式
415 字符串相加（大数加法）竖式加法，对较短的数字进行补0操作 参考2两数相加
470 使用rand7实现rand10 生成一个大范围的随机整数，使用其中一部分